# Webshop Demo

Demo Next.js app. Can be used with API backend from https://github.com/slepix/FlaskDemoAPI.

This app by default exposes TCP port 3000.
Set url of the backend using the `API_URL` environment variable. Example:
```
API_URL=https://api-demo.example.com/api
```
